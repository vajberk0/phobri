<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration for Phobri.
    Allows cleartext for local network communication (pairing via HTTP).
    Self-signed certificates are handled by PairingManager's SSL socket factory.
-->
<network-security-config>
    <!-- Allow cleartext only for local network IPs (pairing endpoint) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.0.0/8</domain>
        <domain includeSubdomains="false">172.16.0.0/12</domain>
        <domain includeSubdomains="false">192.168.0.0/16</domain>
    </domain-config>
</network-security-config>
